# المحفظة الذكية — تعليمات العمل

> **اقرأ `docs/GOVERNANCE.md` قبل أي تغيير.** هو ميثاق **مجلس «نِصاب» للحوكمة
> والاعتماد**، المرجع الأعلى لجودة هذا التطبيق. ما لم يجتز معاييره لا يُسلَّم.

هذا الملف مختصرٌ تشغيلي. المرجع الكامل والمُلزِم هو الميثاق.

## الخطوط الحمراء

1. **سلامة بيانات المحفظة.** لا تعديل ولا ترحيل لأرقام المالك إلا بطلبٍ صريح.
   تعديلاته اليدوية تتقدّم على أي حساب آلي.
2. **لا اختلاق.** ما لا مصدر له في التطبيق يُقال عنه «غير متوفّر».
3. **لا حزمة قبل إذنٍ صريح من المالك.** واسمها دائماً `spupdatechanged.tar.gz`،
   وتُبنى بـ`bash scripts/package.sh` **وحده** — لا بأمرٍ يدويّ. السكربت
   يُشغّل **لجنة كشف الأعطال** أوّلاً ويرفض البناء إن لم تُجتَز، ويطبع
   أوامر التركيب مع الحزمة (بند ٢٩ من الميثاق).
4. **لا مفتاح سرّي** في أمرٍ أو مخرَجٍ أو حزمة.

## المقاييس الموحَّدة

- **صافي الربح** = (القيمة السوقية + النقد) − صافي المساهمات.
- **عائد المحفظة** = صافي الربح ÷ تكلفة المراكز.
- **العائد المركّب** من تاريخ بداية المشروع، ويمتنع دون تسعين يوماً.

## العرض

- محاذاة **اليمين** لكل نصٍّ ورقم. الأرقام لاتينية والنصّ عربية فصيحة.
- الألوان من رموز `frontend/src/styles/globals.css` — لا قيماً ثابتة، والمظهران معاً.
- التباين: أساسي ≥ ١٢:١، ثانوي ≥ ٤٫٥:١. أصغر خطّ ٩٫٥px، أصغر هدف لمس ٣٢px.
- لا حواشي تفسيرية أسفل الشاشات.

## قبل أن تقول «تمّ»

شغّل ما بنيتَه وقِس مخرَجه. اذكر ما تعذّر اختباره وسببه. سمِّ العيب وسببه
الجذري بدل إخفائه تحت «تحسينات».

## طريقةُ الجلب الذكيّ — مسجَّلةٌ لتُنقَل

`docs/FETCH_METHOD.md` — سجلٌّ مستقلٌّ قابلٌ للنقل: انتحالُ بصمة TLS،
واكتشافُ نقطة البيانات من الصفحة، وطبقةُ المتصفّح المقيَّدة، وترتيبُ
الطبقات، وانضباطُ الزمن، والمزالقُ المقيسة. يُقرأ قبل أيّ عملٍ على مصدر،
ويُحدَّث متى تغيّرت الطريقة — وحارسُه `scripts/audit/fetch_method_doc.py`.

## لجنة كشف الأعطال — دورٌ ثابت لا اختياريّ

`bash scripts/audit/run.sh` قبل كل تسليم، و`--live` متى توفّر رمز جلسة.
وكلُّ عطبٍ يُكتشف **يُسجَّل في `scripts/audit/registry.json` ويُولد له
فحص** — لا يُغلق بلاغٌ بلا حارسٍ يمنع عودته. (`scripts/audit/README.md`)

## البنية

- `backend/` — FastAPI · SQLAlchemy async · PostgreSQL
- `frontend/` — React · TypeScript · Vite (عربي RTL)
- `docs/GOVERNANCE.md` — ميثاق مجلس «نِصاب»
- النشر: Docker على AWS EC2 — `/home/ubuntu/Smart-Portfolio`
